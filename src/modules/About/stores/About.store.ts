const state = () => ({});

// getters
const getters = {};

// actions
const actions = {};

// mutations
const mutations = {};

export const aboutStore = {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
