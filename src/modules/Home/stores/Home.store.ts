const state = () => ({});

// getters
const getters = {};

// actions
const actions = {};

// mutations
const mutations = {};

export const homeStore = {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
